# melton basketball stats (static)

this repo is a static replacement for your streamlit dashboard.

## update data
1. replace `Melton Basketball.xlsm`
2. run: `python build_data.py`
3. commit the generated `data/` folder

## github pages
repo settings -> pages -> deploy from branch -> main -> /(root)
